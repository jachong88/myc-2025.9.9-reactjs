/**
 * Routing Components
 * 
 * Provides route-level utilities for authentication and authorization.
 */

export { 
  ProtectedRoute, 
  withProtectedRoute,
  type ProtectedRouteProps 
} from './ProtectedRoute';

export { UnauthorizedPage } from './UnauthorizedPage';
